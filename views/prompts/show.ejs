<!DOCTYPE html>
<html lang="en">
<head>
  <% include ../partials/head %>
</head>

<body class="container-fluid">
  <header>
    <% include ../partials/header %>
  </header>

  <main>

     <h2><%= prompt.promptTheme %></h2>
     <h3 class="promptShowPromptText"><%= prompt.promptText %></h3>
     <h4 class="promptShowPromptText">
          <a href="/stories/new/<%= prompt._id %>" class="btn btn-primary">New Story</a>
          <a href="/prompts/edit/<%= prompt._id %>" class="btn btn-danger">Edit Prompt</a>
    </h4>


<div class='promptShowStoryHook'>
     <% for(var i=0; i< prompt.stories.length; i++) { %>
        <a href="/stories/<%= prompt.stories[i]._id %>"><p><%= prompt.stories[i].storyHook%></p></a>
     <% } %>
     <a href="/stories/new/<%= prompt._id %>" class="btn btn-primary">New Story</a>
  <a href="/prompts/edit/<%= prompt._id %>" class="btn btn-danger">
    Edit Prompt
    <i class="fa fa-cog fa-spin fa-lg fa-fw"></i>
    <span class="sr-only">Loading...</span>
  </a>
</div>

  </main>

  <footer>
    <% include ../partials/footer %>
  </footer>
</body>
</html>
